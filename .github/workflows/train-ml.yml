name: Train ML model (Tempo + Weather + RTE)

on:
  schedule:
    # Tous les jours à 11h UTC (12h FR hiver)
    - cron: "0 11 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  train:
    runs-on: ubuntu-latest

    steps:
      # ======================
      # CHECKOUT
      # ======================
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # 🔑 OBLIGATOIRE pour commit fiable

      # ======================
      # PYTHON
      # ======================
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # ======================
      # INSTALL DEPENDENCIES
      # ======================
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas scikit-learn joblib

      # ======================
      # CHECK DATASET
      # ======================
      - name: Check ML dataset
        run: |
          echo "📂 Vérification du dataset ML"
          ls -lh ML/ml_dataset.json

      # ======================
      # TRAIN MODEL
      # ======================
      - name: Train ML model
        run: |
          python ML/train_ml.py

      # ======================
      # VERIFY MODEL
      # ======================
      - name: Verify ML model
        run: |
          echo "🔍 Vérification du modèle ML"

          if [ ! -f ML/ml_model.pkl ]; then
            echo "❌ ml_model.pkl absent"
            exit 1
          fi

          SIZE=$(stat -c%s ML/ml_model.pkl)
          echo "📦 Taille du modèle : $SIZE bytes"

          if [ "$SIZE" -lt 10000 ]; then
            echo "❌ Modèle trop petit (erreur d'entraînement)"
            exit 1
          fi

          echo "✅ Modèle ML valide"

      # ======================
      # COMMIT MODEL
      # ======================
      - name: Commit trained ML model
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add ML/ml_model.pkl
          git status

          git commit -m "🧠 Retrain ML model (Tempo + Weather + RTE + stress)" || echo "No changes"
          git push
