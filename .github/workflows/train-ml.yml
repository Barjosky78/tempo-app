name: Train ML model (Tempo + Weather + RTE)

on:
  schedule:
    # Tous les jours à 11h UTC (12h FR hiver)
    - cron: "0 11 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  train:
    runs-on: ubuntu-latest

    steps:
      # ======================
      # CHECKOUT
      # ======================
      - name: Checkout repository
        uses: actions/checkout@v3

      # ======================
      # PYTHON
      # ======================
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # ======================
      # INSTALL DEPENDENCIES
      # ======================
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas scikit-learn joblib

      # ======================
      # CHECK DATASET
      # ======================
      - name: Check ML dataset
        run: |
          echo "📂 Vérification du dataset ML"
          ls -lh ML/ml_dataset.json

      # ======================
      # TRAIN MODEL
      # ======================
      - name: Train ML model
        run: |
          python ML/train_ml.py

      # ======================
      # VERIFY MODEL
      # ======================
      - name: Verify ML model
        run: |
          echo "🔍 Vérification du modèle ML"
          if [ ! -f ML/model.pkl ]; then
            echo "❌ Modèle absent"
            exit 1
          fi

          SIZE=$(stat -c%s ML/model.pkl)
          echo "📦 Taille du modèle : $SIZE bytes"

          if [ "$SIZE" -lt 1000 ]; then
            echo "❌ Modèle trop petit (probablement vide)"
            exit 1
          fi

          echo "✅ Modèle ML valide"

      # ======================
      # COMMIT MODEL
      # ======================
      - name: Commit trained model
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ML/model.pkl
          git commit -m "🤖 Auto-train ML model (Tempo + Weather + RTE)" || echo "No changes"
          git push
          
- name: Commit trained ML model
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add ML/ml_model.pkl
          git commit -m "🧠 Retrain ML model with stress features" || echo "No changes"
          git push
