name: Train ML model (Tempo EDF)

on:
  schedule:
    # Tous les jours à 11h UTC (12h heure française en hiver)
    - cron: "0 11 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  train:
    runs-on: ubuntu-latest

    steps:
      # ======================
      # CHECKOUT
      # ======================
      - name: Checkout repository
        uses: actions/checkout@v3

      # ======================
      # SETUP PYTHON
      # ======================
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # ======================
      # INSTALL DEPENDENCIES
      # ======================
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install scikit-learn joblib

      # ======================
      # CHECK DATASET
      # ======================
      - name: Check ML dataset
        run: |
          echo "📂 Vérification du dataset ML"
          if [ ! -f ML/ml_dataset.json ]; then
            echo "❌ ML/ml_dataset.json introuvable"
            exit 1
          fi
          echo "✅ Dataset trouvé"
          echo "📊 Taille :"
          wc -l ML/ml_dataset.json

      # ======================
      # TRAIN MODEL
      # ======================
      - name: Train ML model
        run: |
          echo "🤖 Lancement entraînement ML"
          python ML/train_ml.py

      # ======================
      # VERIFY MODEL
      # ======================
      - name: Verify ML model
        run: |
          echo "🔍 Vérification du modèle ML"
          if [ ! -f ML/model.pkl ]; then
            echo "❌ ML/model.pkl non généré"
            exit 1
          fi

          SIZE=$(stat -c%s ML/model.pkl)
          echo "📦 Taille du modèle : $SIZE bytes"

          if [ "$SIZE" -lt 1000 ]; then
            echo "❌ Modèle trop petit (probablement vide)"
            exit 1
          fi

          echo "✅ Modèle ML valide"

      # ======================
      # COMMIT MODEL
      # ======================
      - name: Commit trained model
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add ML/model.pkl
          git commit -m "🤖 Auto-train ML model (Tempo + météo + RTE)" || echo "No changes"
          git push
